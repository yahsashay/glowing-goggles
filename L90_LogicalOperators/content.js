/**
 * Logical Operators
 */

// && logical And
// || logical Or
// !   logical Not

let myName;
console.log(myName);  // undefined

console.log(!myName) // this will check null or undefined  so it evaluates to true
console.log(myName === undefined || myName === null) // same as above statement
